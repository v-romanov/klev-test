# ВНИМАНИЕ

все команды выполняются из корня репозитория/решения

# `src/First` `test/First`

Первая задача и тесты к ней

Запуск:
```
dotnet test
```

# `src/Second`

Вторая задача, без тесто, просто запускается мейн с потоками записи и чтения

```
dotnet run --project src/Second/Second.csproj
```

# `src/Third` `test/Third`

Третья задача

тесты:
```
dotnet test
```

Недо-cli:

```
dotnet run --project src/Third/Third.csproj
```

Считывает из `./txts/input.txt` и записывает в `./txts/output.txt` и `./txts/problems.txt`